<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wordpress.common.mapper.LinksMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		link_id AS linkId, link_url AS linkUrl, link_name AS linkName, link_image AS linkImage, link_target AS linkTarget, link_description AS linkDescription, link_visible AS linkVisible, link_owner AS linkOwner, link_rating AS linkRating, link_updated AS linkUpdated, link_rel AS linkRel, link_notes AS linkNotes, link_rss AS linkRss
	</sql>

</mapper>